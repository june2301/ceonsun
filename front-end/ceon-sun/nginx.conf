worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    sendfile on;
    keepalive_timeout 65;

    # ======================
    # 1) 80번 포트 (HTTP)
    # ======================
    server {
        listen 80;
        server_name _;

        # certbot 인증서 발급용 (/.well-known/acme-challenge/)
        location /.well-known/acme-challenge/ {
            # certbot에서 webroot 경로로 사용하는 폴더
            root /var/www/certbot;
        }

        # 실제 서비스 API (아직 HTTPS가 없으므로 임시로 80에서 동작)
        location /api/ {
            proxy_pass http://gateway-service:8080/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # 프론트엔드
        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri /index.html;
        }

        error_page 404 /index.html;
    }

    # ======================
    # 2) 443번 포트 (HTTPS)
    # ======================
    # server {
    #     listen 443 ssl;
    #     server_name _;

    #     # 인증서 경로 (발급 후에 셋팅)
    #     ssl_certificate /etc/letsencrypt/live/i12a505.p.ssafy.io/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/i12a505.p.ssafy.io/privkey.pem;

    #     # 실제 서비스 API (HTTPS)
    #     location /api/ {
    #         proxy_pass http://gateway-service:8080/;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }

    #     # 프론트엔드
    #     location / {
    #         root /usr/share/nginx/html;
    #         index index.html;
    #         try_files $uri /index.html;
    #     }

    #     error_page 404 /index.html;
    # }
}
